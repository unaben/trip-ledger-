DATABASE_URL="mysql://trip_calculator:localdevpassword@localhost:3306/trip_calculator"

DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=trip_calculator
DATABASE_PASSWORD=localdevpassword
DATABASE_NAME=trip_calculator

# Same secret works fine for local dev - generate a real one for
# production if you haven't already (see .env.example).
SESSION_SECRET=local_dev_secret_change_me_to_anything_long_and_random
